"""Cameroon Geoportal STAC catalog package.

Import only via the dotted ``catalog.collections`` / ``catalog.register`` paths.
Never put this directory directly on ``sys.path`` — ``collections.py`` would
then shadow Python's standard-library ``collections`` module.
"""
